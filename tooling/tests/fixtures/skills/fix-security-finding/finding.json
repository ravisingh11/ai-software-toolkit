{
  "id": "SEC-001",
  "severity": "P1",
  "status": "open",
  "blocking": true,
  "title": "Shell injection through greet()",
  "surface": "runner.greet",
  "evidence": ["runner.py:6"],
  "impact": "A caller-controlled name such as \"; rm -rf /\" executes arbitrary commands.",
  "fixPlan": "Call subprocess with an argument list and no shell.",
  "verification": {"method": "regression test", "command": "python3 -m unittest discover", "result": "pending", "residualRisk": ""}
}
