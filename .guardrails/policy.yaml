{
  "$schema": "./policy.schema.json",
  "version": 2,
  "name": "baseline",
  "profiles": [
    "core",
    "github"
  ],
  "overrides": {
    "change": {
      "repository-validation": "enforced",
      "format-and-lint": "enforced",
      "migration-validation": "enforced",
      "build": "enforced",
      "unit-tests": "enforced",
      "dependency-remediation": "not_activated"
    },
    "release": {}
  }
}
