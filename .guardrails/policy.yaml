{
  "$schema": "./policy.schema.json",
  "version": 2,
  "name": "baseline",
  "profiles": [
    "core",
    "github"
  ],
  "overrides": {
    "change": {
      "negative-test-unknown-control": "enforced",
      "dependency-remediation": "not_activated"
    },
    "release": {}
  }
}
